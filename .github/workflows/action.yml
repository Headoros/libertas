name: CI/CD for Docker Compose Express App

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build_and_push:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Compose
        uses: docker/setup-buildx-action@v2

    #   - name: Login to Docker Hub
    #     uses: docker/login-action@v2
    #     with:
    #       username: ${{ secrets.DOCKERHUB_USERNAME }}
    #       password: ${{ secrets.DOCKERHUB_TOKEN }}

    #   - name: Build and tag Docker images
    #     run: |
    #       docker-compose build
    #       docker tag your-express-service-name:latest ${{ secrets.DOCKERHUB_USERNAME }}/your-express-service-name:latest

    #   - name: Push Docker images to Docker Hub
    #     run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/your-express-service-name:latest